server:
  port: 8083

spring:
  application:
    name: gateway

  cloud:
    gateway:
      routes:
        - id: patient-service
          uri: http://localhost:8081
          predicates:
            - Path=/patient-service/**
          filters:
            - StripPrefix=1

        - id: note-service
          uri: http://localhost:8084
          predicates:
            - Path=/note-service/**
          filters:
            - StripPrefix=1

management:
  endpoints:
    web:
      exposure:
        include: "*"
