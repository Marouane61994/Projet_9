server:
  port: 8083

spring:
  application:
    name: gateway

  cloud:
    gateway:
      routes:
        - id: patient-service
          uri: http://patient-service:8086
          predicates:
            - Path=/patient-service/**
          filters:
            - StripPrefix=1


        - id: front
          uri: http://front:8082
          predicates:
            - Path=/**

management:
  endpoints:
    web:
      exposure:
        include: "*"
